import type { GameSettings } from "./types"

export const DEFAULT_SETTINGS: GameSettings = {
    theme: "warm",
    mascot: true,
    guardBehavior: "block",
    guardTone: "coach",
}

export const LS_KEY = "doodlelingo_v1"
